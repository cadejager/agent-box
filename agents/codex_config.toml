model = "gpt-oss-64k:20b"
model_provider = "openai" # Force it to use the OpenAI logic

[model_providers.openai]
name = "openai"
# By naming this 'openai', we hijack the standard 'base_url' logic
base_url = "http://host.containers.internal:11434/v1"
api_key = "ollama"